# ========================================
# 운영 환경 설정 (배포 시 사용)
# ========================================

# NFS 저장 설정 (배포 시 주석 해제)
file.upload.enabled.local=false
file.upload.enabled.nfs=true
file.upload.path.nfs=/mnt/nfs/uploads

# 로컬 저장 비활성화
# file.upload.enabled.local=false

# Flask 연동 설정
flask.server.enabled=true
flask.server.url=http://flask-service:5000
